import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    
  },
  textPressPorcent:{
    color:"#ffffff",
    fontSize:40,
  },

  textPressMeta:{
    color:"#ffffff",
    fontSize:15,
  }
});