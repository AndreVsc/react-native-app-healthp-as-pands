import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    containerSettings: {
        backgroundColor:'#fff',
        padding:45,
        paddingTop:30,
        borderRadius:20,
        marginTop:20,
        width:325,
        height:300,
        gap:30,  
  },
});