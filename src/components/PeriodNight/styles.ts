import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex:1,
    flexDirection:'row',
    gap:20,
    maxHeight:30,
    justifyContent:'space-around',
  },

  modalItemText: {
    fontSize: 12,
    color:'#c4c4c4',
  },

});